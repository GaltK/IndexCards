{
  "name": "@indexcards/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for IndexCards",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.2.0"
  }
}
